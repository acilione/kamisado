{
  "name": "kamisado",
  "version": "1.0.0",
  "description": "Multiplayer Kamisado board game with real-time gameplay, sumo mechanics, and chess-style timers",
  "main": "dist/server/index.js",
  "scripts": {
    "build": "tsc && node build-client.js",
    "start": "node dist/server/index.js",
    "dev": "tsx watch src/server/index.ts",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.test.json",
    "test:unit": "tsx tests/unit/test_game_features.ts && tsx tests/unit/test_sumo.ts && tsx tests/unit/test_rules_comprehensive.ts && tsx tests/unit/test_timer.ts",
    "test:integration": "npm run build && node scripts/run-integration-tests.js",
    "test": "npm run typecheck && npm run test:unit && npm run test:integration",
    "ngrok": "node scripts/start-ngrok.js"
  },
  "keywords": [
    "kamisado",
    "board-game",
    "multiplayer",
    "socket.io",
    "typescript"
  ],
  "author": "acilione",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.2.3",
    "esbuild": "^0.27.3",
    "socket.io-client": "^4.7.2",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
